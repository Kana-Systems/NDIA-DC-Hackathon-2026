domain_name        = "ndia.kana.systems"
budget_alert_email = "platform-team@example.mil"
security_domain    = "demo"

# Creates only the secret container. Populate connector credentials separately.
create_graph_connector_secret = false
# graph_ingestion_schedule_expression = "rate(15 minutes)"

# Replace this documentation-only range with known judge or VPN egress CIDRs.
allowed_ingress_cidrs = ["192.0.2.0/24"]

monthly_budget_usd = 250
desired_count      = 1
